import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mindomo.test.app',
  appName: 'mindomo',
  webDir: 'dist/mindomo',
  bundledWebRuntime: false
};

export default config;
